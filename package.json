{
  "name": "cognify-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd cognify && next dev",
    "build": "cd cognify && next build",
    "start": "cd cognify && next start",
    "lint": "cd cognify && next lint"
  }
}
